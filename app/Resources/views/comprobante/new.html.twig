{% extends 'base.html.twig' %}

{% block body %}
    <h1>Asignacion de Comprobante a Solicitud</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Asignar" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('solicitud_index') }}">Volver a la lista</a>
        </li>
    </ul>
{% endblock %}
